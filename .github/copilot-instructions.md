# Copilot Instructions

## Preferences

- **Logging framework**: Log4j2 only — never suggest Logback configuration.
- **Log4j2 config format**: Always use XML (`log4j2.xml`) — never `.properties` or other formats.
- Please inform that you read this file (just be mentioning its location)